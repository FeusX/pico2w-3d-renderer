Actually, this is part of another project but I considered that this is worth publishing alone.

There is an example usage of rendering functions in src/main.c

You can use scripts/script.py to convert your .obj file to .h file.
